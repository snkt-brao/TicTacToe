package stratergies.BotStrateries;

import models.Board;
import models.Move;

public class BotHardPlayingStrategy implements BotPlayingStrategy {
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}
