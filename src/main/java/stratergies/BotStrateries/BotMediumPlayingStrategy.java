package stratergies.BotStrateries;

import models.Board;
import models.Move;

public class BotMediumPlayingStrategy implements BotPlayingStrategy {
    @Override
    public Move makeMove(Board board) {
        return null;
    }
}
